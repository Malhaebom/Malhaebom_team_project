import 'package:flutter/material.dart';

class StoryTestResultPage extends StatelessWidget {
  const StoryTestResultPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}